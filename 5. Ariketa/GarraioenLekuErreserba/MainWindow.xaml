<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GarraioenLekuErreserba"
        xmlns:EserlekuakLibrary="clr-namespace:EserlekuakLibrary;assembly=EserlekuakLibrary" x:Class="GarraioenLekuErreserba.MainWindow"
        mc:Ignorable="d"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Title="Garraio Erresarba" Height="500" Width="500">
    
    <Grid x:Name="MyGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <Border Padding="7" Background="LightGray">
            <StackPanel Orientation="Horizontal" Background="LightGray">
                <Button Content="Autobusa" Click="Autobusa" Margin="5,0" Width="80"/>
                <Button Content="Trena" Click="Trena" Margin="5,0" Width="80"/>
                <Button Content="Hegazkina" Click="Hegazkina" Margin="5,0" Width="80"/>
            </StackPanel>
        </Border>

        <Frame x:Name="MainFrame" NavigationUIVisibility="Hidden" Grid.Row="1"/>
    </Grid>
</Window>
