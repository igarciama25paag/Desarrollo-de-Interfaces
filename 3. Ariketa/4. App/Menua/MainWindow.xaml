<Window x:Class="Menua.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Menua"
        mc:Ignorable="d"
        Title="Textu Editorea" Height="300" Width="300">
    <Grid>
        <Menu x:Name="menu">
            <MenuItem Header="File">
                <MenuItem Header="Itxi" Click="itxi"/>
                <TextBox Width="100"/>
            </MenuItem>
            <MenuItem Header="Edit">
                <MenuItem Header="Cut" InputGestureText="Ctrl+X" Command="ApplicationCommands.Cut"/>
                <MenuItem Header="Copy" InputGestureText="Ctrl+C" Command="ApplicationCommands.Copy"/>
                <MenuItem Header="Paste" InputGestureText="Ctrl+V" Command="ApplicationCommands.Paste"/>
                <MenuItem Header="Delete" InputGestureText="Del" Click="delete"/>
                <TextBox Width="100"/>
            </MenuItem>
            <MenuItem Header="Font">
                <MenuItem Header="Arial" Click="arial"/>
                <MenuItem Header="Courier" Click="courier"/>
                <MenuItem Header="Impact" Click="impact"/>
                <MenuItem Header="Symbol" Click="symbol"/>
                <TextBox Width="100"/>
            </MenuItem>
            <TextBox Width="100"/>
        </Menu>
        <TextBox x:Name="textBox" Margin="5,25,5,5"/>
    </Grid>
</Window>
